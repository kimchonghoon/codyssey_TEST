# Project 2 설계 문서 — 일일 상담 신청 요약 브리핑 (n8n)

## 1. 자동화할 반복 업무 정의

**매일 아침, 누적된 학습상담 신청 현황을 확인하고 요약해서 팀 채널(Slack)에 공유하는 일.**

수작업으로 하면 매일 시트를 열어 신청 건수를 세고, 중등·고등 비중을 확인하고, 핵심을 정리해 공유해야 한다. 매일 반복되는 데다 시간이 들고 누락도 생기기 쉽다. 이 과정을 사람 개입 없이 **정해진 시각에 자동으로** 처리하는 것이 목표다.

---

## 2. 사용 도구 및 선정 이유

**선정 도구: n8n (자가호스팅)**

| 선정 이유 | 설명 |
|---|---|
| **지속 비용 0원** | 매일 도는 작업이라 실행량 과금이 부담될 수 있는데, 자가호스팅 n8n은 실행 횟수와 무관하게 무료 |
| **데이터 자체 보관** | 신청자 개인정보가 외부 클라우드를 경유하지 않고 자체 서버에만 머무름 (프라이버시) |
| **커스터마이즈 자유도** | Code 노드·표현식으로 집계·가공을 세밀하게 제어 |
| **기존 자산 재활용** | Project 1에서 구축한 Google·Slack 자격증명과 `상담관리` 데이터를 그대로 사용 → 빠른 구축 |
| **AI 노드 내장** | Google Gemini 노드를 손쉽게 연결해 AI 요약(보너스)을 자연스럽게 추가 |

---

## 3. 워크플로우 구조 (자동 실행)

```
Schedule Trigger (매일 09:00, 자동)
   → Google Sheets: 상담관리 읽기
   → Code: 집계 (총건수 / 중등 / 고등 / 신청목록)
   → Gemini: 자연어 브리핑 요약  ← AI 보너스
   → IF: 신청 건수 > 0 ?
        ├─ true  → Slack 브리핑 발송 → Google Sheets 브리핑로그 기록
        └─ false → Slack "오늘 신규 없음" 알림
```

**자동 실행 방식**: `Schedule Trigger`로 매일 정해진 시각에 작동하며, 워크플로우를 **Publish(Active)** 상태로 두면 사람이 실행 버튼을 누르지 않아도 자동으로 돈다.

> **[그림 1]** n8n 전체 워크플로우 캔버스 (Schedule → … → 분기 → Slack/기록)
> *(이미지 삽입 위치)*

---

## 4. 노드별 상세

| 단계 | 노드 | 역할 |
|---|---|---|
| 트리거 | **Schedule Trigger** | 매일 09:00 자동 실행 |
| 읽기 | **Google Sheets (Get Rows)** | `상담관리` 탭의 신청 데이터 읽기 |
| 집계 | **Code (JavaScript)** | 총건수·중등·고등 수 계산, 신청 목록 텍스트 생성 |
| AI | **Basic LLM Chain + Google Gemini** | 집계 결과를 자연어 브리핑 문장으로 요약 (보너스1) |
| 분기 | **IF** | 신청 건수 > 0 기준으로 경로 분리 |
| 액션 1 (true) | **Slack — Send a Message** | 요약 브리핑을 채널에 전송 |
| 액션 2 (true) | **Google Sheets — Append Row** | `브리핑로그` 탭에 일시·건수·요약 기록 |
| 액션 (false) | **Slack — Send a Message** | "오늘 신규 없음" 알림 |

> **[그림 2]** 트리거 + 데이터 읽기 (Schedule Trigger / Get Rows 결과)
> *(이미지 삽입 위치)*

> **[그림 3]** Code 노드 집계 결과 (총건수·중등·고등·목록)
> *(이미지 삽입 위치)*

> **[그림 4]** Gemini AI 요약 생성 결과
> *(이미지 삽입 위치)*

---

## 5. 조건 분기 & 양쪽 경로 실행

- **분기 기준**: 신청 건수(`count`) **> 0**
  - **true** — 신청이 있을 때 → 브리핑 발송 + 로그 기록
  - **false** — 신청이 없을 때 → "오늘 신규 없음" 알림
- **양쪽 경로 실행 증거**:
  - true: 실제 데이터(24건)로 실행 → 브리핑 발송 및 로그 기록 확인
  - false: IF 비교값을 일시 조정해 "0건 상황"을 재현 → "없음" 알림 발송 확인

> **[그림 5]** IF 분기 — true 경로 실행 (브리핑 발송)
> *(이미지 삽입 위치)*

> **[그림 6]** true 결과 — Slack 브리핑 + `브리핑로그` 시트 기록
> *(이미지 삽입 위치)*

> **[그림 7]** false 경로 실행 — Slack "오늘 신규 없음"
> *(이미지 삽입 위치)*

---

## 6. 보너스 적용

- **보너스 1 (AI 연동 액션)**: Google Gemini가 신청 목록을 받아 **"오늘 총 OO건(중등 OO·고등 OO), 특징 한 줄"** 형태의 자연어 브리핑을 자동 생성. ✓ 적용
- 보너스 2 (실패 알림·재시도): 이번 범위에서는 미적용 — 향후 확장 항목.

> **[그림 8]** Publish / Active 상태 (자동 실행 설정 완료)
> *(이미지 삽입 위치)*

---

## 7. 요건 충족 체크

| 요건 | 충족 |
|---|---|
| 트리거 1개 이상 | ✓ Schedule Trigger |
| 액션 2개 이상 | ✓ Slack 브리핑 + Google Sheets 기록 |
| 조건 분기 1개 이상 | ✓ IF (건수 기준) |
| 각 분기 경로 1회 이상 실행 | ✓ true·false 모두 확인 |
| 자동 실행 | ✓ Schedule + Active |
| 보너스 (AI 액션) | ✓ Gemini 요약 |

---

## 8. 보안

- 자격증명(Google OAuth, Gemini API 키, Slack 봇 토큰)은 비공개로 관리하고 캡처 시 마스킹했다.
- 테스트에는 실제 개인정보 대신 더미 데이터를 사용했다.

---

## 9. 향후 확장 (다음 단계)

- 실시간 처리: n8n을 공개 호스팅하고 Supabase Database Webhook과 연결해 검사 완료 즉시 작동.
- 발송 채널 고도화: 검사 결과를 학부모에게 **카카오 알림톡**(솔라피)으로 자동 발송하고, 일정 시간 뒤 상담 안내 메시지로 자연스럽게 상담 전환하는 QuadY 상담 퍼널로 확장.
- 보너스 2: 알림톡 실패 시 SMS 대체발송 + 실패 알림으로 안정성 강화.
