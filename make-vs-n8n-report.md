# 자동화 도구 비교 구현 보고서 — Make vs n8n

## 1. 개요

동일한 자동화 워크플로우를 서로 다른 두 도구(**Make**, **n8n**)로 구현하고, 구현 과정과 도구별 특성을 비교 분석한다.

- **사용한 도구**
  - **Make** (클라우드형 SaaS, 무료 플랜)
  - **n8n** (자가호스팅, `localhost:5678`)
- **구현한 워크플로우 (공통)**
  > 구글 폼 상담 신청 → 응답 시트에 새 행 추가 → **학년 기준 분기(중등 / 고등)** → 상담관리 시트에 정리 기록 + Slack 채널 알림
- **공통 구조 요건 충족**: 트리거 1개 · 액션 2개 이상(시트 기록 + Slack 알림) · 조건 분기 1개(학년) · 양쪽 분기 경로 모두 실제 실행

---

## 2. 구현한 워크플로우 구조

| 단계 | 내용 |
|---|---|
| 트리거 | 구글 폼 응답이 쌓이는 시트에 **새 행이 추가**되면 작동 |
| 분기 | `학년`이 **"중"으로 시작** → 중등 경로 / **"고"로 시작** → 고등 경로 |
| 액션 1 | `상담관리` 시트에 정리 행 기록 (`구분` 칸에 중등/고등 표시) |
| 액션 2 | Slack `#새-채널`에 신청 내용 알림 전송 |

> **[그림 1]** 워크플로우 흐름도 (트리거 → 분기 → 시트/Slack)
> *(이미지 삽입 위치)*

---

## 3. 도구별 구현 과정

### 3.1 Make

1. **트리거**: `Google Sheets → Watch New Rows`
   - Search Method: Select by path / 응답 시트 선택
   - Table contains headers: Yes / **Row with headers: `A1:E1`** (헤더 범위를 A1 표기로 지정해야 함)
2. **분기**: `Flow Control → Router` 추가 후, 두 경로(line)에 각각 **필터** 설정
   - 1st 경로: `학년` **Starts with** `중` (중등)
   - 2nd 경로: `학년` **Starts with** `고` (고등)
3. **액션**: 각 경로에 `Google Sheets → Add a Row`(상담관리 기록, `구분` 고정값) → `Slack → Send a Message`(채널 알림)
4. **테스트**: `Run once`로 실행, 중등·고등 두 경로가 모두 실행됨을 확인

> **[그림 2]** Make 전체 시나리오 구성 (트리거 → Router → 시트/Slack)
> *(이미지 삽입 위치)*

> **[그림 3]** Make 분기 필터 설정 (Starts with 중 / 고)
> *(이미지 삽입 위치)*

> **[그림 4]** Make 실행 결과 — 상담관리 시트 기록 + Slack 알림
> *(이미지 삽입 위치)*

### 3.2 n8n (자가호스팅)

자가호스팅 특성상 **자격증명을 직접 발급**하는 사전 작업이 필요했다.

- **구글 인증**: Google Cloud Console에서 프로젝트 생성 → Google Sheets API · Google Drive API 사용 설정 → OAuth 동의 화면(External, 테스트 사용자 등록) → **OAuth 클라이언트(웹 애플리케이션)** 생성, 리디렉션 URI `http://localhost:5678/rest/oauth2-credential/callback` 등록 → Client ID/Secret 발급
  - *(서비스 계정은 Sheets Trigger 노드 미지원 + 최근 생성분은 내 드라이브 접근 불가로 OAuth2 채택)*
- **Slack 인증**: Slack 앱 생성 → Bot Token Scopes(`chat:write`, `chat:write.public`, `channels:read`) 추가 → 워크스페이스 설치 → **봇 토큰(`xoxb-…`)** 발급

본 구현:

1. **트리거**: `Google Sheets Trigger → On row added` (응답 시트, Poll: Every Minute)
2. **분기**: `Switch`(Mode: Rules)
   - Rule 1: `{{ $json["2.학년"] }}` **starts with** `중` → 출력 `중등`
   - Rule 2: `{{ $json["2.학년"] }}` **starts with** `고` → 출력 `고등`
3. **액션**: 각 출력에 `Google Sheets → Append Row`(상담관리, `구분` 고정값) → `Slack → Send a Message`
4. **테스트**: `Test workflow`로 전체 실행, 7건이 중등(4)·고등(3)으로 갈라져 양쪽 모두 실행됨을 확인

> **[그림 5]** n8n 자격증명 설정 (Google Cloud OAuth 클라이언트 / Slack 봇 토큰)
> *(이미지 삽입 위치)*

> **[그림 6]** n8n 전체 워크플로우 구성 (Trigger → Switch → 시트/Slack)
> *(이미지 삽입 위치)*

> **[그림 7]** n8n Switch 분기 설정 (Rules)
> *(이미지 삽입 위치)*

> **[그림 8]** n8n 실행 결과 — 상담관리 시트 기록 + Slack 알림
> *(이미지 삽입 위치)*

---

## 4. 비교 분석

| # | 비교 항목 | Make | n8n (자가호스팅) |
|---|---|---|---|
| 1 | **호스팅 / 배포** | 클라우드 SaaS, 설치 불필요 — 가입 즉시 사용 | 직접 서버/도커로 운영(본 구현은 localhost) |
| 2 | **인증·자격증명 설정** | **관리형 OAuth** — 구글·슬랙을 클릭 몇 번으로 연결 | **직접 발급** — 구글 Cloud OAuth 클라이언트 생성 + 슬랙 봇 토큰 발급 필요 (가장 큰 차이) |
| 3 | **UI / UX** | 시각적 모듈 + 라인, 라인 위에 필터·라우터 배치 | 노드 캔버스 + Switch/IF 노드, 값은 표현식 기반 |
| 4 | **분기(조건) 구현** | Router + 경로별 필터(Starts with 등) | Switch 노드 Rules(조건 + 출력 이름 지정) |
| 5 | **필드 매핑 방식** | 출력 토큰을 드래그 | `{{ $json["..."] }}` 표현식 / 입력 패널 드래그 |
| 6 | **실행 · 로그 확인** | `Run once` + 실행 히스토리, 모듈별 번들 데이터 확인 | 노드별 `Execute step` + `Test workflow`, 입·출력 패널(Schema/Table/JSON) |
| 7 | **무료 플랜 / 비용** | 무료 월 1,000 ops, 활성 시나리오 2개, 최소 15분 간격 | 자가호스팅 시 실행·워크플로우 사실상 무제한(인프라·운영은 본인 부담) |
| 8 | **데이터 흐름 / 프라이버시** | 데이터가 Make 클라우드를 경유 | 데이터가 본인 서버에만 머무름 |

---

## 5. 장단점 정리

### Make
- **장점**: 시작이 매우 빠름(관리형 OAuth), 직관적인 시각 UI, 라인 기반 필터로 분기가 한눈에 보임, 설치·운영 부담 없음, 풍부한 커넥터.
- **단점**: 무료 ops·시나리오 수 제한(실행량 많아지면 과금), 데이터가 외부(Make 클라우드)를 경유, 세밀한 커스터마이즈는 상대적으로 제한적.

### n8n (자가호스팅)
- **장점**: 자가호스팅 시 무료·무제한 실행, 데이터가 자체 서버에만 머물러 프라이버시에 유리, 표현식 기반의 강력한 커스터마이즈, 오픈소스.
- **단점**: 초기 자격증명 설정이 복잡(구글 OAuth 클라이언트·슬랙 봇 토큰 직접 발급), 서버 운영·유지보수 부담, 트리거/액션 자격증명 타입 분리 등 학습 곡선, OAuth 테스트 모드는 토큰이 7일마다 만료(재연결 필요).

---

## 6. 어떤 상황에 적합한가 (의견)

- **Make가 적합한 경우**: 비개발자가 **빠르게** 자동화를 시작하고 싶을 때, 설치·서버 운영을 피하고 싶을 때, 실행량이 적거나 중간 수준일 때, 여러 SaaS를 클릭만으로 연결하고 싶을 때.
- **n8n이 적합한 경우**: 어느 정도 기술 역량이 있고, **데이터를 외부로 보내지 않고 자체 보관**해야 할 때(프라이버시·보안), 대량 실행을 **비용 없이** 돌리고 싶을 때, 표현식·커스텀 로직으로 세밀하게 제어하고 싶을 때.

> 요약: **"빠르고 쉽게"는 Make, "무제한·자체 통제"는 n8n.** 동일한 워크플로우라도, 진입장벽은 Make가 낮고, 자유도·비용 효율은 n8n이 높았다.

---

## 7. 무료/과금 및 보안

- **두 도구 모두 유료 결제 없이 무료 범위 내에서 완수**했다. Make는 무료 플랜 ops 범위 내에서, n8n은 자가호스팅으로 비용이 발생하지 않았다.
- 제출물의 민감정보(OAuth Client Secret, Slack 봇 토큰, 전화번호 등)는 마스킹 처리했으며, 테스트에는 실제 개인정보 대신 더미 데이터를 사용했다.
